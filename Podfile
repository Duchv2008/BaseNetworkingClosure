# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'BaseNetworking' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  pod 'ObjectMapper'
  pod 'Alamofire'
  # Pods for BaseNetworking

  target 'BaseNetworkingTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'BaseNetworkingUITests' do
    # Pods for testing
  end

end
